query Teams {
  teams {
    nodes {
      id
      name
      key
      description
    }
  }
}

query TeamMemberships($teamId: String!) {
  team(id: $teamId) {
    id
    name
    memberships {
      nodes {
        user {
          id
          displayName
        }
      }
    }
  }
}
